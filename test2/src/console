<?php

require dirname(__DIR__) . '/vendor/autoload.php';

use AvanaAssignment\Commands\ValidateCommand;
use Symfony\Component\Console\Application;

$application = new Application();

$application->add(new ValidateCommand());

$application->run();
